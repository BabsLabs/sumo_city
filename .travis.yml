language: ruby
rvm:
  - 2.7.0
services:
  - postgresql
  - xvfb
env:
  - DB=postgresql
script:
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rake db:test:prepare
  - bundle exec rake
before_script:
  # - psql -c 'create database sumocity_travis_test;' -U postgres 
test:
  adapter: postgresql
  database: sumocity_travis_test
dist: trusty
addons:
  chrome: stable
before_install:
  - # start your web application and listen on `localhost`
  - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9222 http://localhost &